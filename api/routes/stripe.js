import express from "express"
// import { createPaymentIntent, helloStripe, paymentIndent } from "../controllers/cardController.cjs";

const router = express.Router();

// router.post("/create-payment-intent", paymentIndent)
// router.post("/create-payment-intent", createPaymentIntent)
// router.get("/", helloStripe)

export default router